import 'package:flutter/material.dart';

class PieChart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        height: 50,
        width: 50,
        color: Colors.grey,
//      decoration: BoxDecoration(
//        shape: BoxShape.circle, boxShadow: customShadow, color: primaryColor, borderRadius: BorderRadius.circular(5),
//      ),
      //child: Container(),
    );
  }
}
